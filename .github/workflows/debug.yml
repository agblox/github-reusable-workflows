name: debug

on:
  workflow_call:
    inputs:
      flag:
        default: false
        description: Boolean flag
        required: false
        type: boolean

jobs:
  flag:
    runs-on: ubuntu-latest
    name: Test boolean flag
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v2
      - if: inputs.flag
        run: echo 'flag value ${{ inputs.flag }}'
#      - if: ! inputs.flag
#        run: echo 'flag value ${{ inputs.with-flag }}'
